node_modules/
dist/
build/
.next/
*.tsbuildinfo
*.js.map
*.d.ts.map

# Environment — never commit
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
/tmp/

# OS
.DS_Store
Thumbs.db

# Build cache
.cache/
coverage/
.tsx/

# Local data dirs (not project knowledge)
/data/

# .kap/ is tracked: kap.json, decisions/, updates/ are the public PKG
# Ignore only local binary / cache artifacts if present under .kap
.kap/**/*.db
.kap/**/*.db-wal
.kap/**/*.db-shm
.kap/.cache/

# Claude Code local settings — may contain tokens
.claude/settings.json
.claude/settings.local.json

# Local MCP config — may contain tokens
.mcp.json

# Test files
test-sampling.js
test-github-pkg.js
